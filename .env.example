# Optional — live shopping search (not wired yet; mocks always work)

# Provider-specific keys for a future Google Shopping / SerpAPI integration
GOOGLE_SHOPPING_API_KEY=
SERPAPI_API_KEY=

# Mirrored to the client only to show “live vs mock” UI hints
NEXT_PUBLIC_GOOGLE_SHOPPING_API_KEY=
